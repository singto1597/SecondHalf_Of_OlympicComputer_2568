#include <bits/stdc++.h>

using namespace std;

int main(){
    int n;
    cin >> n;
    int d[n], score[n];
    for (int i = 0; i < n; i ++){
        cin >> d[i] >> score[i];
    }
    int time, skill, m;
    cin >> time >> skill >> m;





}